[pytest]
# Put the repo root on sys.path so `import atf_pipeline` works without an
# install or PYTHONPATH. The unit suite uses only synthetic ATF snippets, so it
# needs no corpus data and runs in well under a second.
pythonpath = .
testpaths = tests
addopts = -q
